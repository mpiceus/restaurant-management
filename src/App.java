/**
 * Entry point tiện cho VS Code (default package).
 * Khuyến nghị chạy `controller.Main` nếu bạn dùng package.
 */
public class App {
    public static void main(String[] args) {
        controller.Main.main(args);
    }
}

